# Crawler v2 runtime dependencies
# Install with:
#   pip install -r crawler_v2/requirements.txt

# Core async HTTP + crawler runtime
aiohttp>=3.9.0
loguru>=0.7.0
python-dotenv>=1.0.0

# Database access
sqlalchemy>=2.0.0
pymysql>=1.1.0

# Anti-block and schema detection
fake-useragent>=1.5.0
selectolax>=0.3.21

# Optional test dependencies (uncomment if running tests)
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
